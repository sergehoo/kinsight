# URL de base de l'API governance (backend Django).
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Jeton JWT de dev (facultatif) pour appeler l'API réelle depuis le navigateur.
VITE_API_TOKEN=

# Mettre à 'true' pour afficher des données d'exemple sans backend (démo/dev).
VITE_USE_MOCK=false
